# ========================================
# Production Environment Configuration
# ========================================
# This is a TEMPLATE - actual values come from Secret Manager
# DO NOT commit real credentials to Git

# Environment
ENV=PROD
GOOGLE_CLOUD_PROJECT=datascientest-460618

# BigQuery (from Secret Manager)
BQ_PROJECT=${SECRET:bq-project-bike}
BQ_RAW_DATASET=${SECRET:bq-raw-dataset-bike}
BQ_PREDICT_DATASET=${SECRET:bq-predict-dataset-bike}
BQ_LOCATION=${SECRET:bq-location}

# GCS (from Secret Manager)
GCS_BUCKET=${SECRET:gcs-bucket-bike}

# API (from Secret Manager)
API_URL=${SECRET:prod-bike-api-url}
API_KEY_SECRET=${SECRET:bike-api-key-secret}

# Training Data (from Secret Manager)
TRAIN_DATA_PATH=${SECRET:train-data-path}
TEST_DATA_PATH=${SECRET:test-data-path}

# MLflow (from Secret Manager)
MLFLOW_TRACKING_URI=http://mlflow:5000
MLFLOW_DB_HOST=${SECRET:mlflow-db-host}
MLFLOW_DB_NAME=${SECRET:mlflow-db-name}
MLFLOW_DB_USER=${SECRET:mlflow-db-user}
MLFLOW_DB_PASSWORD=${SECRET:mlflow-db-password}

# Google credentials (via service account)
GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-production.json

# ========================================
# How to use in production:
# ========================================
# Option 1: Load from Secret Manager in code
#   from utils.env_config import get_env_config
#   config = get_env_config()  # Automatically loads from Secret Manager
#
# Option 2: Cloud Run environment variables
#   gcloud run services update SERVICE_NAME \
#     --set-secrets="MLFLOW_DB_PASSWORD=mlflow-db-password:latest"
#
# Option 3: Direct secret access in Python
#   from google.cloud import secretmanager
#   password = get_secret("mlflow-db-password")
